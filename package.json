{
  "name": "ung-dung-da-nen-tang-quan-ly-sinh-vien",
  "version": "1.0.0",
  "description": "Xây dựng ứng dụng đa nền tảng quản lý sinh viên - Web & Mobile",
  "scripts": {
    "start": "npm run dev",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix web-app\"",
    "install-all": "npm install && npm install --prefix server && npm install --prefix web-app"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
